# s3-rds-glue-pipeline
🚀 Fault-tolerant cloud data pipeline: Reads CSV from S3, loads into RDS (MySQL), and automatically falls back to AWS Glue if the database fails. Dockerized ETL workflow for production-style DevOps and Data Engineering.
